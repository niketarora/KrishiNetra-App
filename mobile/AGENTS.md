# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.
The .env files were provided by my friend and are now placed exactly as:
- mobile/.env
- backend/.env

Continue from here. Verify that both files are detected without displaying any secret values, then start the backend and Expo app and try to open the app on my physical Android phone first. If that doesn't work, use the Android emulator.

Do not modify application functionality or add any features yet.