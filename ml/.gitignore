# Regenerated by `python -m src.preprocessing` — large and fully reproducible
# from the raw CSV, so it is not worth versioning.
data/processed/

# Local virtual environment
.venv/
__pycache__/
*.pyc

# Trained model binaries. The metrics, reports and metadata that describe them
# ARE committed; the pickles themselves are rebuilt by
# `python -m src.train --stage final`.
models/*.pkl
